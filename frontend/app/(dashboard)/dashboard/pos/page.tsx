import { PosTerminal } from "@/frontend/components/dashboard/pos/pos-terminal";

/** POS Lite (Phase C11). In-store cashier terminal — build a ticket, charge, print. */
export default function DashboardPosPage() {
  return <PosTerminal />;
}
