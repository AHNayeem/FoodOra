import { QrStudio } from "@/frontend/components/dashboard/qr/qr-studio";

/** QR Menu studio (Phase C12). Generate, preview, download and print table codes. */
export default function DashboardQrPage() {
  return <QrStudio />;
}
