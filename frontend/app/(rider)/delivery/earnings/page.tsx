import { EarningsView } from "@/frontend/components/rider/earnings-view";

/** Delivery earnings for today, the week and the month (Phase C18). */
export default function RiderEarningsPage() {
  return <EarningsView />;
}
